
// Show the approved_check.png image
$('div.loan_status').children().replaceWith("<%= j image_tag 'approved_check.png', width: "150" %>");


// Show the reject button
$('#the_buttons').children().first().replaceWith("<%= j render partial: 'btn_reject' %>");