
// Show the approved_check.png image
$('div.loan_status').children().first().replaceWith("<%= j render 'approve_the_loan' %>");


// Show approved icon
$('#loan_approval_link').replaceWith("<%= j render partial: 'approved' %>");


// Show reject button
$('#rejected-icon').replaceWith("<%= j render partial: 'btn_reject' %>")