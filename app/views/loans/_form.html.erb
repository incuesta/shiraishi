<%= form_with(model: loan, local: true) do |form| %>
  <% if loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div>
      <%= form.label :loan_type_id %>
      <%= form.select :loan_type_id, LoanType.all.map { |loan_type|  [loan_type.name, loan_type.id]  } %>
    </div>


    <div class="field">
      <%= form.label :principal_amount %>
      <%= form.text_field :principal_amount %>
    </div>

  

    <div class="field">
      <%= form.label :status %>
      <%= form.select :status, [["disburse", "disbursed"], ["reject", "rejected"]] %>
    </div>



    <div class="field">
      <%= form.label :notes %>
      <%= form.text_area :notes, id: :loan_notes %>
    </div>


    <div class="actions">
      <%= form.submit %>
    </div>
<% end %>
